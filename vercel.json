{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && (npx prisma migrate deploy || echo '⚠️ migrate skipped — DB already up to date or unreachable at build') && npx next build"
}
